// const player =`Player 1`;
const player = prompt(`Entrez votre nom :`, `Player1`);
const png = `Computer`;
const max = 3;

pName.innerText = player;
const getRandomInt = (max) => {
    return Math.floor(Math.random() * Math.floor(max));
  }

